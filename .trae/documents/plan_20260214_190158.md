Saya telah selesai menambahkan fitur gambar produk.

**Ringkasan Pekerjaan:**
1.  **Database**: Menambahkan kolom `image` (tipe text) ke tabel `products`.
2.  **Schema**: Mengupdate `shared/schema.ts` agar API bisa menerima field `image`.
3.  **Inventory (Admin)**: Mengupdate form tambah/edit produk di `Inventory.tsx` untuk menyertakan input URL gambar dengan preview sederhana.
4.  **POS (Frontend)**: Mengupdate `ProductCard.tsx` untuk menampilkan gambar produk di bagian atas kartu jika tersedia.

Sekarang Anda bisa memasukkan URL gambar saat menambah atau mengedit produk di menu Inventory, dan gambar tersebut akan muncul di halaman kasir (POS).

Apakah ada lagi yang ingin Anda tambahkan terkait fitur produk ini?